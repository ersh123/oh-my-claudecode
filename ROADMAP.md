# OMC Trust Roadmap

This is the steering artifact required by the autonomous mandate. Mark an area done only when its exit criteria are checked by current evidence, not by intent.

| Area | Exit criteria | Status | Evidence / next action |
| --- | --- | --- | --- |
| nikoflow | [x] Human gates require a post-request real user turn. [x] Execute/verify gates require independent reviewer tool results. [x] Stale request ids fail closed. [x] Request-id fidelity is pinned. | Done | Fresh focused nikoflow evidence covers post-mint user turns, no-user/self-confirm rejection, request-id rotation, stale-id rejection, prompt injection, reviewer role fidelity, and property mismatch checks. |
| ralph | [x] Cannot self-approve. [x] Phase machine cannot livelock on recoverable failures. [x] Live Stop-bridge dogfood has BLOCK->PASS evidence. | Done | Ralph verification rejects copied approval text, stale request ids, non-reviewer native Task `agent_type`, final-rejection verification reminting, and false PASS extraction from multiline BLOCK entries. Live compiled Stop bridge blocked on `<ralph-verification>`, then passed after critic `tool_result` and cleared Ralph verification state. |
| team | [x] Team phases reach terminal states. [x] Worker launch/model routing is deterministic. [x] Worktree cleanup is covered. | Done | Spawn env baseline fixed; cleanup now removes safe clean worktrees even when unrelated backup blockers keep team state. Runtime-v2 monitor now persists terminal `phase-state.json` for completed/failed task sets. Claude, Codex, Gemini, Grok, and Antigravity worker launch args collapse duplicate/conflicting model flags to one canonical `--model <value>` with last explicit launch model winning. |
| persistent-mode | [x] Runtime hook cannot silently skip active nikoflow missing-engine enforcement. [x] Runtime/template missing-engine parity is checked. [x] Failure mode is explicit in tests. [x] Live plugin Stop manifest smoke covers real hook config. | Done | Runtime and installed template both fail closed for missing TS engine; live manifest-shaped Stop command blocks active nikoflow state in a temp git project/state. Broader template/runtime parity stays under `.mjs` parity. |
| state IO | [ ] Cross-process writes are atomic where needed. [ ] RMW order is designed for nikoflow F1. [ ] No user work is lost on crash/retry. | Escalation-gated | F1 is design-then-STOP before production edits. |
| gate detection | [x] Reviewer channel accepts only review-capable Task/proxy_Task/Agent tool results. [x] Main-thread text never passes review gates. [x] Tests pin negative cases. | Done | Transcript reviewer gates are nikoflow and ralph. Nikoflow rejects Bash/main-thread/non-reviewer Task laundering; ralph rejects copied text, stale ids, and non-reviewer native Task `agent_type` while preserving reviewer `subagent_type` and `agent_type`. |
| transcript scan | [x] Transcript parsing handles hook noise. [x] Required BLOCK and PASS quotes are extracted without leaking sensitive values. [x] Local runtime paths are redacted in committed artifacts. | Done | `extractDogfoodTranscriptEvidence` parses JSONL/raw hook noise, requires separate BLOCK and later PASS quotes, and redacts token/session/profile paths before journal use. |
| `.mjs` parity | [ ] Hook/runtime `.mjs` defaults match TS engine. [ ] Build scripts keep generated runtime in the same commit as source changes. [ ] Drift checks are mechanical. | In progress | Canonical `team-*` phase fallback fixed in TS helper/PreToolUse `.mjs`; installed template keyword hook now matches runtime `/ask` provider suppression; installed post-tool failure hook now matches runtime startup/noisy-scan suppression; installed session-start template now matches runtime safe prior-session ultrawork restore wording; standalone code-simplifier template now shares Windows child-process hide hardening with the runtime script and lint coverage. `scripts/uninstall.sh` now cleans current standalone `.mjs` hook files and filters current manifest hook names from manual settings without deleting third-party hooks. Next: continue systematic `.mjs`/TS default audit. |
| docs | [ ] User docs match current commands and gates. [x] Mandate-required roadmap and journal are committed. [x] Recovery via `loop-last-good` is documented. | In progress | `docs/GETTING-STARTED.md` documents the `loop-last-good` branch/tag recovery anchor and `npm run test:baseline`; `tier0-docs-consistency` pins recovery strings, hook manifest count / Stop script names, Node hook-runtime platform docs, the `REFERENCE.md` skill table against `listBuiltinSkillNames({ includeAliases: true })`, and Slash Commands rows against bundled `commands/*.md` wrappers. `omc-doctor` legacy-content inventories are pinned against bundled `agents/`, `skills/`, and `commands/`. `skill-docs-contract` guards active skill docs against the removed `/oh-my-claudecode:note` command; `public-docs-command-contract` guards public docs and `skills/AGENTS.md` against advertising removed `note`, `omc-help`, and `ralph-init` command names, and pins `seminar/quickref.md` Key Commands to bundled skills or `commands/*.md` wrappers. `skills-agents-docs-contract` pins the `skills/AGENTS.md` headline count, existing `*/SKILL.md` paths, exhaustive Key Files coverage, Key Files Skill cells against frontmatter `name:`, Key Files Purpose cells against frontmatter `description:`, Auto-Activation coverage for bundled skills with real frontmatter `triggers:`, and machine-readable Skill Categories coverage for all bundled skill dirs without inline annotations or duplicate resolved dirs. Next: broader command/gate docs accuracy and deeper category trigger wording parity. |

## Backlog

- Design nikoflow F1 write ordering and stop before production edits.
- Audit `.mjs` defaults and parsers against TS runtime.
- Extend livelock coverage across team and persistent-mode.

## Cadence

- Every iteration updates this file when it changes area status or backlog.
- Every 10th iteration audits the least-recently-audited area and records evidence in `AUTONOMOUS-JOURNAL.md`.
